{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.14.0",
    "netlify-plugin-prisma-provider": "^0.3.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  },
  "scripts": {
    "install": "yarn install",
    "build": "yarn rw db up --no-db-client && yarn rw build"
  }
}
